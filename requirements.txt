# Core ML and Data Processing Libraries
# IMPORTANT: These versions are pinned to match KServe SKLearn server v0.12.0
# Do not upgrade these versions as it will cause model loading failures
pandas>=1.1.0,<2.0.0
numpy==1.24.3
scikit-learn==1.3.2
joblib>=1.0.0

# HTTP Client for API calls
requests>=2.25.0

# Optional: For better output formatting
tabulate>=0.8.0
